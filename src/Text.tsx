

const Text = () => {
    return (
        <div>
             action dispatch reducer payload initialState store increment decrement 
             action dispatch reducer payload initialState store increment decrement 
             action dispatch reducer payload initialState store increment decrement 
             action dispatch  reducer payload initialState store increment decrement
             action dispatch reducer payload initialState store increment decrement
             action dispatch reducer payload initialState store increment decrement 
             action dispatch reducer payload initialState store increment decrement
             action dispatch reducer payload initialState store increment decrement 
             action dispatch reducer payload initialState store increment decrement 
             action dispatch reducer payload initialState store increment decrement 
             action dispatch reducer payload initialState store increment decrement
              
        </div>
    );
};

export default Text;